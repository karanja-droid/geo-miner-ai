import React from 'react';
import { Typography, Box } from '@mui/material';

const Admin: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Admin</Typography>
    <Typography>Admin features coming soon.</Typography>
  </Box>
);

export default Admin; 